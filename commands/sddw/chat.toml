prompt = """
<feature_name> {{args}} </feature_name>

# COMMON RULES
!{cat ${extensionPath}/instructions/common.md}

# INSTRUCTIONS
!{cat ${extensionPath}/instructions/chat.md}

# SPECS
!{cat ${extensionPath}/specs/requirements.md}
!{cat ${extensionPath}/specs/code-analysis.md}
!{cat ${extensionPath}/specs/design-task.md}
!{cat ${extensionPath}/specs/task-completion.md}
"""
